# docker-compose.yml (skeleton — teammates fill in backend/db)
services:
  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    environment:
      - VITE_API_BASE_URL=http://backend:5000/api

  # backend:
  #   build: ./backend
  #   ports:
  #     - "5000:5000"
  #   environment:
  #     - DB_HOST=db
  #   depends_on:
  #     - db

  # db:
  #   image: mysql:8
  #   environment:
  #     - MYSQL_DATABASE=restaurant_app
  #     - MYSQL_ROOT_PASSWORD=${DB_ROOT_PASSWORD}
  #   volumes:
  #     - db_data:/var/lib/mysql

# volumes:
#   db_data: